calcfi:
  baseUrl: "https://calcfi.com"
  symbols: ["series"]
  concurrency: 2
  retry: {maxAttempts: 3, backoffMs: 800, maxBackoffMs: 8000}
  rateLimit: {qps: 2, burst: 4}
  paths: {bronze: "data/bronze/calcfi", silver: "data/silver/calcfi", catalog: "catalog/glue.json"}
