sina:
  baseUrl: "https://hq.sinajs.cn"
  symbols: ["sh600000"]
  concurrency: 2
  retry: {maxAttempts: 3, backoffMs: 800, maxBackoffMs: 8000}
  rateLimit: {qps: 2, burst: 4}
  paths: {bronze: "data/bronze/sina", silver: "data/silver/sina", catalog: "catalog/glue.json"}
