{
  "private": true,
  "type": "module",
  "description": "hedge-fund monorepo - commitlint + husky",
  "scripts": {
    "prepare": "husky",
    "commitlint": "commitlint --from=origin/main --to=HEAD --verbose",
    "commitlint:current": "commitlint --from=HEAD~1 --to=HEAD --verbose"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.0",
    "husky": "^9.1.7"
  },
  "engines": {
    "node": ">=18"
  }
}
